services:
  backend:
    image: 720800607391.dkr.ecr.us-east-1.amazonaws.com/electronix-ecr:latest
    container-name: electronix-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart : unless-stopped
